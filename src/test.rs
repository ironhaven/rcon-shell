//use facio::client::RconClient;
fn main() {
/*
	let mut rcon = RconClient::open("127.0.0.1:25575",
                        "password",
                        <Option<&str>>::None,
                        //Some("seed"),
                        None).expect("Cannot open rcon");

	// Helps prevent "connection reset" errors on the socket
	std::thread::sleep(std::time::Duration::from_secs(1));
	
	println!("{}", rcon.exec("list").unwrap());
*/
}
